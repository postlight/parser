# scripts that run after cloning repository
install:
  # by default, all script lines are interpreted as batch

build:
  parallel: true                  # enable MSBuild parallel builds


# to run your custom scripts instead of automatic tests
test_script:
  - Install-Product node 4.3.2 && npm run build && jest --maxWorkers=1
  - Install-Product node 6.9 && npm run build && jest --maxWorkers=1
  - Install-Product node 7.0 && npm run build && jest --maxWorkers=1
